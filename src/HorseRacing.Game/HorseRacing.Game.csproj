<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!-- Mutators stay internal: only a meeting may run a race in a real game.
         The tests need them to force a result. -->
    <InternalsVisibleTo Include="HorseRacing.Game.Tests" />
  </ItemGroup>

</Project>
